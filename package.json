{
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "upload:docker": "docker buildx build --platform linux/amd64,linux/arm64 -t geraldf/nextjs-docker . && docker push geraldf/nextjs-docker"
  },
  "dependencies": {
    "next": "latest",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
